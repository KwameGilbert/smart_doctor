; Hoist all packages to node_modules root — needed for Expo CLI tools
; (expo-doctor, expo config) to resolve their internal dependencies correctly
; when using pnpm's non-flat node_modules layout.
shamefully-hoist=true
